# Build custom Codex client with Electrobun

## Context
The current Codex macOS app has UX friction points that slow down daily work. We want a custom macOS client built with Electrobun while keeping Codex model performance and capabilities.

The recommended approach is App Server-first (not SDK-first): use `codex app-server` over `stdio`, and bundle the Codex runtime so the app does not depend on the Codex desktop app being installed or running.

## Objective
Ship a macOS-only Electrobun Codex MVP that supports core interactive coding flows through App Server and runs without requiring the Codex desktop app.

It should replace Codex macOS app.

## Subtasks
- [x] Scaffold Electrobun app shell with renderer/main-process split and stable local dev run command.
- [x] Integrate Codex App Server process lifecycle in the main process (`spawn`, health checks, restart strategy, graceful shutdown).
- [x] Implement protocol client over `stdio` with typed request/response wrappers for thread start, run, stream events, and approvals.
- [x] Build first-pass UX for threads, live streaming output, command/file approvals, and error recovery states.
- [ ] Bundle Codex runtime with the app artifact and verify fresh-machine startup with no Codex desktop install.
- [ ] Define fallback path for API-only mode (Responses API) and capture scope delta versus App Server mode.

## Progress Log
### 2026-02-24
- Task created
- Implemented windowed Electrobun app shell (`src/bun` + `src/mainview`) with local API/WebSocket bridge.
- Wired Codex runtime state into live UI controls (boot, thread start, prompt submit, approve/deny).
- Added automatic `app-server` -> `proto` runtime fallback for newer Codex CLI builds and validated end-to-end run flow.

## Notes
Constraints:
- macOS only
- speed of iteration matters more than protocol stability
- no dependency on Codex desktop app process

Decision rule:
- use App Server as primary integration surface
