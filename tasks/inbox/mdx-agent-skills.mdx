---
title: "mdx agent skills"
priority: low
tags: []
---

# We should use .mdx for "agent skills" (term for local in-repo tasks; simply "skills" refer to our app's domain).

We should use .mdx for "agent skills" (term for local in-repo tasks; simply "skills" refer to our app's domain).

So we can re-use pieces of instructions with `<Block />`. We're already building them anyway.
