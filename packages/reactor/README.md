# Reactor Placeholder

This workspace replaces the previous broken `prototypes/reactor` gitlink.

The original gitlink pointed at commit:

```text
7581a718cfc7454e9ccf3030e2c7b2e6ca44c1d2
```

No `.gitmodules` mapping or local submodule metadata was present in this checkout, so the source cannot be restored automatically. Keep this placeholder as the workspace boundary until the real Reactor source is available.
