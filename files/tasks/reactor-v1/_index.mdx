---
title: Reactor v1
priority: high
tags: [status:active, class:task]
---

This is the main v1 task.

Meseeks v0 has been the exploration phase. Meseeks v1 is the first public-beta shape: new core execution engine, new pricing/accountability model, clearer branding, and a product surface that can support real users. The new Reactor is the gating piece. Most of the current reorganization exists so this work can be seen, planned, and executed cleanly.

different implementations per provider?
like GPT specific?

---

# Reactor

The engine. The platform.

Use the current execution references in this folder as source material:

- [Current action execution model](action-execution-model/_index.md)
- [Action execution flows](action-execution-flow/_index.md)

They describe v0/current behavior and may be stale as implementation changes. Keep useful details, but v1 should define the new core instead of preserving accidental current constraints.

- files
	- virtual tasks, skills
	- support "bash pipe", multi-action per request
- learning
- we must support synchronous actions
- we must support multiple tool-call
- safe secrets
- paralell execution
	- per depth?
	- it should be able to have long-running stuff, like Codex
	- we could have like a "timeout trigger"
- testable as a unit
- more robust authorization
	- support for OAuth, OIDC and other auth types
	- specific controls per skills (like search authorizes per domain)
	- authorization triggers for action execution
		- trigger authorization from runtime conditions like `contextLength > X`, `executionTime > 60 * 1000`, or `domain is whitelisted`
- native CoT support?
	- sort of like a multi-reaction action? simply that?
	- 5.4 reasons, becomes a subaction. if it call tools, then it becomes a subaction. etc.
- multi-reactions as a whole
	- hard (code) skills can also trigger multi-reactions
- triggers primitive
	- loops could be just a set of triggers (maybe with a core/entry soft skill that we loop on)
	- "every 5 rounds"
	- "on context > X%"
- support for infinite tasks/duties: goals that never really complete, such as "stay in touch with my grandma" or "get fit"
- skill grouping!
	- this is very important
	- built-in stuff should be versioned btw
		- innate/say@0.1.0
		- core/say@0.1.0
		- pro/searchWeb@0.1.0
	- managed intelligence aliases/wrappers
		- simpler picker names such as `@pro/efficient` instead of exposing the exact current model, provider, version, and reasoning mode
		- aliases act like managed symlinks: `@pro/efficient` can point to Kimi K2.5 today, then K2.6 or another better fit tomorrow
		- aliases can be owned by orgs, e.g. `@pro/efficient`, `@openai/GPT:latest`, `@xai/Grok-fast:latest`
		- users should be able to pin exact models when they care, but default UX should prefer stable capability names
- version everything: tasks, skills, ...
	- `updateInstructions()` should compute and persist a git-style diff at write-time, then render that diff to humans and future AI context instead of only saying that a title/body changed
	- task history should make updates versioned, easy to visualize, and reversible
- live UI customization
	- Meseeks should let users select a running UI/component and ask the AI to change its appearance or behavior: bigger, smaller, different color, rounder, different interaction, etc.
	- MDX/component rendering should make the app surface directly customizable at runtime
	- reference: [Agentation](../../references/agentation/_index.md)
- bi-rendering
	- important components/actions/skills should be able to render for humans and for AI
	- human rendering targets UI; AI rendering targets model context, Markdown, XML-ish structure, or `ModelMessage[]`
	- MDX should be able to mention task/action outputs, and those references must render into the AI-facing instructions/context
	- this overlaps with [Bi-rendering system](../bi-rendering-system/_index.md), but the Reactor framing is broader: rendering is part of the engine contract, not just action-history cleanup
	- references: JSX prompts in practice, [jsx-prompts](https://fables.gg/blog/introducing-jsx-prompts-build-complex-prompts-for-llms-with-jsx), [Priompt](https://github.com/anysphere/priompt), [mdx-prompt](https://github.com/edspencer/mdx-prompt)
- external task interfaces
	- any task can request incoming email addresses; each received email becomes a message/action on that task
	- tasks can also have outgoing email addresses when they need to send email
	- the same primitive should support HTTP endpoints, webhooks, WebSocket streams, Telegram-style integrations, and future external systems
	- incoming external events become task messages/actions; the task loop/triggers decide whether reactions run and what happens next
	- task activity is the shared history of all actions taken by humans, AI, skills, and external systems on that task
- redesign our skills to prompt using `<xml>` tags
- `instruct()` should likely become `plan()`. I'm not sure about `task.instructions`; could be `task.plan` or simpy `task.description` or even `.body`.
- sharing skills
	- at least some basic importing/exporting support
	- skill marketplace/subscriptions
		- people can offer skills they have evolved over time
		- installed skill instructions are probably public/inspectable after install
		- because skills keep evolving, users can subscribe to a source and be notified when an update is available; their Pro can update for them when policy allows
		- skills can be free or paid
		- skill authors can proxy API keys and charge usage, with or without markup (or make it free)
		- first-party `@pro` skills can proxy services like Tavily so customers get useful defaults out of the box; if we charge exactly provider cost, the accounting still goes through energy
		- third-party skills may add markup for risk, scarcity, service quality, or business model reasons
- subtasks
	- I should be able to create and move freely, controlling reactions
- keep "magic rock" naming for fun
	- including the comment in the file
- energy as a first-class citizen
	- estimated cost should be "max theoretical cost"
		- based on maxOutputTokens, fixed costs, and so on
		- for a hard skill, should predict the cost for the sandbox config in use (timeout time, max RAM usage, etc)
	- the model should see the cost of each skill and preemptively suggest other paths that could require more energy
	- done() could have a mandatory confidence: 0-100 field (so we could trigger a continue)
	- doubleCheck() starting from X energy
		- when a done() call is made, run a doubleCheck() to accept/reject it
	- properly account reasoning tokens
	- variable hard-skill costs
- multi-provider
	- code sandbox
	- LLM/AI
	- storage
	- 💡 we could support "provider credits", like what we have with Daytona
		- this could also be very useful for accounting!
		- so we need "provider" as a primitive, and trace usage to it
- ETH deposits
	- simplest possible version: accept ETH deposits into the user/task wallet, not checkout-style payments
	- wallet balance then pays for models, sandboxes, storage, skills, and external providers through the same accounting layer
	- support for x402
	- [CDP Wallets](https://www.coinbase.com/en-pt/developer-platform/discover/launches/cdp-wallets-launch)
	- [Coinbase Onramp API](https://x.com/coinbasedev/status/1942617509112213740?s=12&t=fpfVVVXvGdro5tlVeroG9A)
	- [Coinbase embedded wallets](https://x.com/brian_armstrong/status/1978703400008122612?s=46&t=fpfVVVXvGdro5tlVeroG9A)
- notifications
	- time-sensitive
- support for sync/pre-executed action
- take ActionCtx only (not MutationCtx anymore)
- maybe we need minimal support for TRANSactions?
	- so actions that affects multiple tasks are visible on both?
	- possible requirement for subtasks
	- maybe actions involving multiple tasks or actors are "transactions"
	- transaction is the proof: a transaction should make the cross-task/actor effect traceable wherever it matters
- branching
- standardized solution for huge responses
	- e.g. persisting to filesystem and feeding it a path it can read with tail/head/cat
- searchWeb could take multiple queries at once
- multi-player
	- fix authorization from the beginning
	- portable agent identity/trust/reputation may need ERC-8004-style discovery, registration files, MCP/A2A endpoints, on-chain reputation, validators, wallet/payment proofs, and x402-weighted feedback; reference: [ERC-8004 agent trust and discovery](../../references/erc-8004-agent-trust-and-discovery/_index.md)
- root task should be "Igor's life"

# Virtual File System

- [S3 files as shared agent workspace](https://x.com/skeptrune/status/2041657917708103915?s=12): Nick Khami's point was that S3 files could remove the need to spin up a sandbox VM just to give agents POSIX-like tools, and could let many compute workers operate over the same filesystem.
- [Rivet operating system](https://x.com/nathanflurry/status/2039017546649907674?s=12): Rivet described a WASM/V8-isolate environment with `node`, `python`, `bash`, `git`, `grep`, `curl`, mounts for S3/Google Drive/SQLite, library embedding, Actors, Secure Exec, and Sandbox Agent.
- [JavaScript runtime inside BEAM](https://x.com/dan_note/status/2032139121850728939?s=12): Danila Poyarkov described JS runtimes as supervised GenServers with native BEAM term mapping, Elixir-owned DOM reading, OXC TypeScript tooling, npm package support, and sandboxed user-defined business rules.
- [Microsandbox](https://x.com/microsandbox/status/2045114415016522062?s=12): fast local sandboxes for agents; useful reminder that execution should not always require cloud infrastructure.
- [Zeroboot](https://x.com/githubprojects/status/2034955640872083785?s=12): preloaded VM snapshots that fork new isolated VMs in about 0.8ms; relevant to keeping sandbox startup invisible.
- [Persistent sprites / full Linux environments](https://x.com/martin_casado/status/2014452233384169791?s=20): Martin Casado framed the future as many persistent AI compute sprites with internet access, generated UI, checkpointing, and full Linux environments.
- [Rivet Sandbox Agent SDK](https://x.com/rivet_dev/status/2016548084696727842?s=46): a universal HTTP/TypeScript API for running coding agents such as Claude Code, Codex, OpenCode, and Amp inside sandboxes.
- [Sim / Freestyle server-side JS execution](https://github.com/simstudioai/sim/blob/main/sim/app/api/function/execute/route.ts): original Sim route now 404s, but the imported note also pointed at `https://api.freestyle.sh` as a related custom server-side JS execution surface.
- [Tencent/E2B-compatible microVM sandbox](https://x.com/ericksky/status/2046569383149932750?s=12): claimed self-hosted open-source sandbox with sub-60ms startup, roughly 5 MB RAM per instance, KVM/RustVMM isolation, and E2B SDK compatibility.
- [Owned DOM sandbox via QuickJS/WASM](https://x.com/jpschroeder/status/2036115634749112744?s=12): parse TypeScript AST `html` calls, replace reactive parts with identifiers, run JS inside WASM/QuickJS, and restrict DOM changes to generated owned blocks mounted through Web Components/Shadow DOM.
- [Wasmer Edge.js](https://x.com/wasmerio/status/2033966082944577693?s=12): run unmodified Node.js apps safely without Docker, with a sandboxed design and pluggable JS engine.
- [Rivet Secure Exec SDK](https://x.com/rivet_dev/status/2034295508446187619?s=20): secure Node.js execution as a library, supporting Node.js, Bun, and browsers with Cloudflare-Workers-like isolation, low cold start, and low memory.

## Nice to have

- some streaming support?
	- check Convex persistent streaming thing
	- maybe a switch between "reading" and "writing" (after it starts streaming back)?

## Current Issues

- Failed to persist initial action details for m973c5rz0br28x3v4t8vtqq2k982tb2c:' [Error: Uncaught Error: Action detail already exists\n at handler (../../convex/action/details.private.ts:28:54
- Write conflicts
- I should be able to "discard" a closed task? Or Resolve if discarded?
	- should I rename those to "Archive" / "Complete" or smth?
- Compose() should be renamed to build()
	- and it should default to being a soft skill loop as a sub task!
		> call build()
		> build() starts a subtask with Build loop
		> say("Working on it, will keep you posted.")
- we really need BYOK for the transparency
- consider using an Agent kit such as Pi or AI SDK or OpenAI Agents SDK

## Architectural Ideas

- External Fan-Out Queue (like Vercel Queues)
- Env Safety
	- Perplexity uses a proxy `agent-proxy.perplexity.ai` with their own API key
	- OpenAI/Anthropic-compatible proxies so they set BASE_URL to it
	- we could do similar and "run the action" in the background?!
- retry on technical failures
- we should probably be using "aggregators" for computed stuff like task status? not sure
- look into modern AI SDK and TanStack AI; consider choosing one as a native layer
- Actions should likely have a maximum size (because of context)
	- and/or/at least expose a "size" thing we can do math on

---

Chapter 1: the platform
	- tasks, actions, reactions, skills, loops, etc.
	- macOS app
	- iOS app
Chapter 2: multiplayer
	- the programmatic interface 
		- email as first class citizen
		- http api, open responses, websocket
	- shared resources
	- realtime collaboration
	- chat (DMs)
	- human verification
Pahse 3: 

meta/microsoft:

- spot market opportunity
- build dark pattern software around it
- burn money to monopolize that industry
- work as hard as possible to LOCK YOU IN

> China and US get unimaginably rich
> US starts buying poorest countries
> they do great
> small countries start to sell off to US
> a big chunk of that income flows to the new economy (distributed, decentralized, open source, etc)
> China starts expanding too
> new econ is already >15% of the global GDP

> US, China and new econ.

> US and China is all that left, weirdly distributed

	x Europe? North Korea? The arabs?

> all extreme shit (religions, centralized states, )

> our take into AI is like owning a car
> you OWN it, you do whatever the F you want with it, and you're responsible for the outcomes
