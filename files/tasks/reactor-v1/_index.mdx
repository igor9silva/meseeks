---
title: Reactor v1
priority: high
tags: [status:active, class:task]
---

This is the main v1 task.

Meseeks v0 has been the exploration phase. Meseeks v1 is the first public-beta shape: new core execution engine, new pricing/accountability model, clearer branding, and a product surface that can support real users. The new Reactor is the gating piece. Most of the current reorganization exists so this work can be seen, planned, and executed cleanly.

different implementations per provider?
like GPT specific?

---

# Reactor

The engine. The platform.

This task should become the implementation-grade technical design for V2: explicit enough that we can give Codex one large goal and have it produce working code. It is not a phased roadmap; it is the contract for the next core.

## Execution Contract

Reactor has three durable phases:

1. `claim`
2. `perform`
3. `finish`

`claim` prepares durable action details, computes cost fields, checks task policy, checks account balance, checks authorization when needed, reserves funds/energy, marks the action running, and schedules or invokes execution.

`perform` marks `startedAt`, rebuilds execution context from durable action details, and runs the skill/action.

`finish` settles cost, persists result/cost metadata, updates pressure, emits reactions, and attempts to claim the next runnable action.

Accounting vocabulary:

- `maxCost`: hard upper bound.
- `estimatedCost`: current best prediction.
- `reservedEnergy`: account hold/reservation.
- `actualCost`: real settled cost.

`action_details` stays as the durable details table. Reactor owns the helper/write lifecycle around it: details are persisted before authorization, reservation, and scheduling; execution rebuilds context from those persisted details instead of relying on transient runtime state.

Loop and trigger model:

- `action.loop` passes through reactions.
- `task.triggers` are always live.
- `task.triggers` can override loop triggers.
- Loops are collections of triggers, not active runtime entities.
- No `chain`.
- No `chainId`.
- No active-loop object.

Interruption semantics:

- Convex actions are not cancellable.
- Started interrupted actions finish financially and settle real costs.
- Started interrupted actions do not react.
- Started interrupted actions should disappear from future model history.
- Unstarted interrupted actions can be skipped without cost.

Cost rules:

- Soft-skill cost should use prepared input plus capped max output, not the whole possible context window.
- If `actualCost > maxCost`, Reactor should warn or enforce loudly. Silent overrun is not acceptable.

Bi-rendering belongs in Reactor at the high level: one canonical artifact, many render targets. Actions should be able to produce artifacts that render for humans, render for AI context, and expose file/path handles when the payload is too large or media-like to inline.

Dead concepts that should not come back:

- `claimed` as a status; use timestamps instead.
- `budgetScope`.
- task-wallet accounting.
- chain budget abstractions.
- `reactionTrigger`, `shouldReact`, or `finishTrigger` as durable concepts.
- verbose helper/type names such as `buildMagicRockContextFromActionDetails`, `ClaimAuthorization`, `reactorBudget`, `reactorDetails`, `spentEnergy`, or `nullResultSchema`.

- files
	- virtual tasks, skills
	- support "bash pipe", multi-action per request
	- files should have the dream tagging system, like what I always wanted from macOS; the Organizer experiment makes this feel like the right approach
	- rename `references` to `memories`
- learning
	> "if you ask AGI something it don't know how to do, it should be able to just learn how to do it, and then do it (given enough resources)"
- we must support synchronous actions
	- built-in stuff should run directly instead of scheduling an action when scheduling is unnecessary (sync actions)
- we must support multiple tool-call
- safe secrets
- paralell execution
	- per depth?
	- it should be able to have long-running stuff, like Codex
	- we could have like a "timeout trigger"
- testable as a unit
- more robust authorization
	- support for OAuth, OIDC and other auth types
	- specific controls per skills (like search authorizes per domain)
	- authorization triggers for action execution
		- trigger authorization from runtime conditions like `contextLength > X`, `executionTime > 60 * 1000`, or `domain is whitelisted`
- native CoT support?
	- sort of like a multi-reaction action? simply that?
	- 5.4 reasons, becomes a subaction. if it call tools, then it becomes a subaction. etc.
- multi-reactions as a whole
	- hard (code) skills can also trigger multi-reactions
- triggers primitive
	- loops could be just a set of triggers (maybe with a core/entry soft skill that we loop on)
	- schedules in the new Reactor should be implemented using triggers
	- "every 5 rounds"
	- "on context > X%"
	- trigger usage should be a counter, not just a boolean: a trigger can be allowed to fire N times, then deactivate/remove itself
- one-off tasks
	- AI should be able to start one-off tasks itself: child tasks that run a loop, eventually finish, and can auto-resolve without direct user interaction
	- useful for parallel work, better organization, and isolating small internal work units without forcing the user to manage every subtask
	- system-wide dictation is a good example: each dictation creates a raw voice file, then triggers transcription into a second file
	- raw audio and transcription files should have specific tags so we can render a dedicated dictation/transcription UI showing every captured transcription, with retries, corrections, yada yada
- support for infinite tasks/duties: goals that never really complete, such as "stay in touch with my grandma" or "get fit"
- task shape, to consider ??
	- use `task.goal` for the definition of done: the thing we test against to decide whether a task is complete
	- use `task.plan` for optional planning/current context as the task evolves; `.plan` should include the task summary
	- the word `goal` already has community momentum from Codex `/goal`, which we can reuse as vocabulary
- loop behavior examples to preserve
	- respect explicit output contracts from the triggering user action, even when the loop does internal research or planning
	- example failure: user asked "Qual deixa broxa, finasterida ou minoxidil? Responde só o nome mais nada."; the loop correctly researched, but replied with explanation and sources instead of only the medication name
- skill grouping!
	- this is very important
	- every app feature should be exposed to Meseeks as a skill; no hidden human-only capability
	- feedback/reporting is part of that: if the user complains that Meseeks is broken, the AI should be able to ask permission and call a feedback tool that reports the actual problem to us
	- built-in stuff should be versioned btw
		- innate/say@0.1.0
		- core/say@0.1.0
		- pro/searchWeb@0.1.0
	- managed intelligence aliases/wrappers
		- simpler picker names such as `@pro/efficient` instead of exposing the exact current model, provider, version, and reasoning mode
		- aliases act like managed symlinks: `@pro/efficient` can point to Kimi K2.5 today, then K2.6 or another better fit tomorrow
		- aliases can be owned by orgs, e.g. `@pro/efficient`, `@openai/GPT:latest`, `@xai/Grok-fast:latest`
		- users should be able to pin exact models when they care, but default UX should prefer stable capability names
- version everything: tasks, skills, ...
	- actions need `action.index`: a stable per-task counter so summaries and diffs can reference ranges such as "actions 1 to 100"
	- mutations should render git-style patches to the model: e.g. `updateInstructions()` should compute and persist a diff at write-time, then render the previous-vs-updated patch to humans and future AI context instead of the whole new instruction body
	- task history should make updates versioned, easy to visualize, and reversible
- live UI customization
	- Meseeks should let users select a running UI/component and ask the AI to change its appearance or behavior: bigger, smaller, different color, rounder, different interaction, etc.
	- MDX/component rendering should make the app surface directly customizable at runtime
	- component catalog/playground: users should be able to view MDX-renderable components individually, test props/classes, and set per-user global defaults
	- customization should be inheritable through the task hierarchy: a root-level override affects child tasks, while a lower task-level override affects only that subtree
	- reference: [Agentation](../../references/agentation/_index.md)
- bi-rendering
	- important components/actions/skills should be able to render for humans and for AI
	- human rendering targets UI; AI rendering targets model context, Markdown, XML-ish structure, or `ModelMessage[]`
	- consider actions returning component definitions instead of raw payloads
		- render the component for humans through React
		- render the same component for digital intelligences in whatever format they support
		- this could solve huge-result context pollution by returning a component with a virtual file path or another lazy-readable handle instead of the full content
	- MDX should be able to mention task/action outputs, and those references must render into the AI-facing instructions/context
	- this overlaps with [Bi-rendering system](../bi-rendering-system/_index.md), but the Reactor framing is broader: rendering is part of the engine contract, not just action-history cleanup
	- references: JSX prompts in practice, [jsx-prompts](https://fables.gg/blog/introducing-jsx-prompts-build-complex-prompts-for-llms-with-jsx), [Priompt](https://github.com/anysphere/priompt), [mdx-prompt](https://github.com/edspencer/mdx-prompt), [Smithers durable JSX AI workflow](../../references/smithers-durable-jsx-ai-workflow/_index.md)
- external task interfaces
	- any task can request incoming email addresses; each received email becomes a message/action on that task
	- tasks can also have outgoing email addresses when they need to send email
	- the same primitive should support HTTP endpoints, webhooks, WebSocket streams, Telegram-style integrations, and future external systems
	- incoming external events become task messages/actions; the task loop/triggers decide whether reactions run and what happens next
	- task activity is the shared history of all actions taken by humans, AI, skills, and external systems on that task
- redesign our skills to prompt using `<xml>` tags
- It should have a `<Constitution version="latest" />` instruction. "Forced" into every layer.
	- maybe the task hierarchy is enough, as instructions drill down
- `instruct()` should become `plan()`
- Rename reason() to reasoning()
- consider renaming say() to message(), write(), comment() or other
- sharing skills
	- at least some basic importing/exporting support
	- skill marketplace/subscriptions
		- people can offer skills they have evolved over time
		- installed skill instructions are probably public/inspectable after install
		- because skills keep evolving, users can subscribe to a source and be notified when an update is available; their Pro can update for them when policy allows
		- skills can be free or paid
		- skill authors can proxy API keys and charge usage, with or without markup (or make it free)
		- first-party `@pro` skills can proxy services like Tavily so customers get useful defaults out of the box; if we charge exactly provider cost, the accounting still goes through energy
		- third-party skills may add markup for risk, scarcity, service quality, or business model reasons
- subtasks
	- I should be able to create and move freely, controlling reactions
- keep "magic rock" naming for fun
- UX should treat `#` as task mention syntax:
	- `#` is for mentioning tasks. Period.
	- Launcher: after `#` it starts searching for tasks by "anything"
	- Composer: any `#` searches in-place for tasks to mention
	- potential for "any #" in Launcher as well?
- @ is for entities (people and orgs)
- experiment with parenthesis skill invocation instead of slash commands: typing something like `say(` could open the skill list after `(`; decide through UX testing
	- original note: no / shit! just `(`	- including the comment in the file
- energy as a first-class citizen
	- `maxCost` should be max theoretical cost
		- based on maxOutputTokens, fixed costs, and so on
	- `estimatedCost` is separate: current best prediction, only when we have a real estimate
		- for a hard skill, should predict the cost for the sandbox config in use (timeout time, max RAM usage, etc)
	- the model should see the cost of each skill and preemptively suggest other paths that could require more energy
	- done() could have a mandatory confidence: 0-100 field (so we could trigger a continue)
	- doubleCheck() starting from X energy
		- when a done() call is made, run a doubleCheck() to accept/reject it
	- properly account reasoning tokens
	- variable hard-skill costs
	- unresolved recurring maintenance costs
		- how do we charge for costs that keep accruing over time, like file storage/R2?
		- maybe "maintenance cost" is the thing we charge every X time, but the accounting model is still unclear
	- unresolved low account balance behavior
		- what happens when account balance is too low to fulfill work that has already been committed?
		- maybe a `deactivate()` / `clearEnergy()`-style trigger when `accountBalance < threshold`, enforced on every loop as a protection
- multi-provider
	- code sandbox
	- LLM/AI
	- storage
	- 💡 we could support "provider credits", like what we have with Daytona
		- this could also be very useful for accounting!
		- so we need "provider" as a primitive, and trace usage to it
- ETH deposits
	- consider supporting ETH deposits from day 1
	- simplest possible version: accept ETH deposits into the user/task wallet, not checkout-style payments
	- wallet balance then pays for models, sandboxes, storage, skills, and external providers through the same accounting layer
	- support for x402
	- [CDP Wallets](https://www.coinbase.com/en-pt/developer-platform/discover/launches/cdp-wallets-launch)
	- [Coinbase Onramp API](https://x.com/coinbasedev/status/1942617509112213740?s=12&t=fpfVVVXvGdro5tlVeroG9A)
	- [Coinbase embedded wallets](https://x.com/brian_armstrong/status/1978703400008122612?s=46&t=fpfVVVXvGdro5tlVeroG9A)
- notifications
	- time-sensitive
	- PWA notifications
	- basic unread mechanics
	- minimal offline support could make the web app a decent v1 by itself
- support for sync/pre-executed action
- take ActionCtx only (not MutationCtx anymore)
- maybe we need minimal support for TRANSactions?
	- so actions that affects multiple tasks are visible on both?
	- possible requirement for subtasks
	- maybe actions involving multiple tasks or actors are "transactions"
	- transaction is the proof: a transaction should make the cross-task/actor effect traceable wherever it matters
- branching
- standardized solution for huge responses
	- e.g. persisting to filesystem and feeding it a path it can read with tail/head/cat
	- hard skills can return huge payloads; one observed result was around 455,810 characters (http://localhost:3000/task/kh70t75w7zpw3yykvc6b2298w585xrkn?debug=true#nd7e8m7xd03tb98yjsb1aec9gh85xxm0), which wrecks model context and is not useful inline
	- after the virtual filesystem exists, huge action results should be written to a virtual file and surfaced as a readable handle/path
	- the model should choose whether to read specific lines, `head`, `tail`, summaries, or slices instead of being forced to ingest the whole result
- the loop should auto-manage context distribution
	- decide whether to use a larger-context model variant
	- choose the distribution, e.g. less message history but more images, or drop a file for more message history
- searchWeb could take multiple queries at once
- launcher search should be scored, not grouped
	- example: if `isDiscard`, multiply score by `0.2`
- UX: `#` is for mentioning tasks. Period.
	* Launcher: `after # it starts searching for tasks by "anything"`
	* Composer: `any # searches in-place for tasks to mention`
	* potential for "any  #" in Launcher ask well?
- multi-player
	- fix authorization from the beginning
	- portable agent identity/trust/reputation may need ERC-8004-style discovery, registration files, MCP/A2A endpoints, on-chain reputation, validators, wallet/payment proofs, and x402-weighted feedback; reference: [ERC-8004 agent trust and discovery](../../references/erc-8004-agent-trust-and-discovery/_index.md)
- root task should be "Igor's life"
- bring your own keys

# Virtual File System

- private inbox items tagged `human:vfs` are the review queue for this section: candidates, references, and related information for the virtual filesystem implementation
- [Files SDK](../../references/files-sdk/_index.md): unified storage SDK for S3/R2/Vercel Blob/Google Drive-style backends and agent SDK tools; relevant to a provider-neutral VFS layer.
- run the entire action inside Daytona
	- persistent file system on S3-like
	- if we do git-versioning everything, consider `repositories` as a first-class primitive
	- run multiple actions per invocation?
	- safe internet access and code running
	- sandboxes could have a lifecycle independent of actions, priced after the used timeout; visible in a composer strip
	- files also exist within the app, attached to the task
	- can be attached to multiple tasks, both point to each other
	- dedicated files UI, everything gets processed and organized
	- everything attached gets mounted
	- how to handle ephemeral data? like git clones
	- maybe just "task_data/", which gets persisted and mounted, but is not exposed to "Files", which are user-uploaded or explicitly saved
	- idk how to deal with auth within the sandbox, like API keys we have for skills
- [S3 files as shared agent workspace](https://x.com/skeptrune/status/2041657917708103915?s=12): Nick Khami's point was that S3 files could remove the need to spin up a sandbox VM just to give agents POSIX-like tools, and could let many compute workers operate over the same filesystem.
- [Rivet operating system](https://x.com/nathanflurry/status/2039017546649907674?s=12): Rivet described a WASM/V8-isolate environment with `node`, `python`, `bash`, `git`, `grep`, `curl`, mounts for S3/Google Drive/SQLite, library embedding, Actors, Secure Exec, and Sandbox Agent.
- [JavaScript runtime inside BEAM](https://x.com/dan_note/status/2032139121850728939?s=12): Danila Poyarkov described JS runtimes as supervised GenServers with native BEAM term mapping, Elixir-owned DOM reading, OXC TypeScript tooling, npm package support, and sandboxed user-defined business rules.
- [Microsandbox](https://x.com/microsandbox/status/2045114415016522062?s=12): fast local sandboxes for agents; useful reminder that execution should not always require cloud infrastructure.
- [Zeroboot](https://x.com/githubprojects/status/2034955640872083785?s=12): preloaded VM snapshots that fork new isolated VMs in about 0.8ms; relevant to keeping sandbox startup invisible.
- [Persistent sprites / full Linux environments](https://x.com/martin_casado/status/2014452233384169791?s=20): Martin Casado framed the future as many persistent AI compute sprites with internet access, generated UI, checkpointing, and full Linux environments.
- [Rivet Sandbox Agent SDK](https://x.com/rivet_dev/status/2016548084696727842?s=46): a universal HTTP/TypeScript API for running coding agents such as Claude Code, Codex, OpenCode, and Amp inside sandboxes.
- [Sim / Freestyle server-side JS execution](https://github.com/simstudioai/sim/blob/main/sim/app/api/function/execute/route.ts): original Sim route now 404s, but the imported note also pointed at `https://api.freestyle.sh` as a related custom server-side JS execution surface.
- [Tencent/E2B-compatible microVM sandbox](https://x.com/ericksky/status/2046569383149932750?s=12): claimed self-hosted open-source sandbox with sub-60ms startup, roughly 5 MB RAM per instance, KVM/RustVMM isolation, and E2B SDK compatibility.
- [Owned DOM sandbox via QuickJS/WASM](https://x.com/jpschroeder/status/2036115634749112744?s=12): parse TypeScript AST `html` calls, replace reactive parts with identifiers, run JS inside WASM/QuickJS, and restrict DOM changes to generated owned blocks mounted through Web Components/Shadow DOM.
- [Wasmer Edge.js](https://x.com/wasmerio/status/2033966082944577693?s=12): run unmodified Node.js apps safely without Docker, with a sandboxed design and pluggable JS engine.
- [Rivet Secure Exec SDK](https://x.com/rivet_dev/status/2034295508446187619?s=20): secure Node.js execution as a library, supporting Node.js, Bun, and browsers with Cloudflare-Workers-like isolation, low cold start, and low memory.

## Nice to have

- some streaming support?
	- check Convex persistent streaming thing
	- maybe a switch between "reading" and "writing" (after it starts streaming back)?

## Current Issues

- Failed to persist initial action details for m973c5rz0br28x3v4t8vtqq2k982tb2c:' [Error: Uncaught Error: Action detail already exists\n at handler (../../convex/action/details.private.ts:28:54
- Write conflicts
- I should be able to "discard" a closed task? Or Resolve if discarded?
	- should I rename those to "Archive" / "Complete" or smth?
- Compose() should be renamed to build()
	- and it should default to being a soft skill loop as a sub task!
		> call build()
		> build() starts a subtask with Build loop
		> say("Working on it, will keep you posted.")
- we really need BYOK for the transparency
- consider using an Agent kit such as Pi or AI SDK or OpenAI Agents SDK

## Architectural Ideas

- External Fan-Out Queue (like Vercel Queues)
- Env Safety
	- Perplexity uses a proxy `agent-proxy.perplexity.ai` with their own API key
	- OpenAI/Anthropic-compatible proxies so they set BASE_URL to it
	- we could do similar and "run the action" in the background?!
- retry on technical failures
- we should probably be using Convex"aggregators" for computed stuff like task status? not sure
- look into modern AI SDK and TanStack AI; consider choosing one as a native layer
- Actions should likely have a maximum size (because of context)
	- and/or/at least expose a "size" thing we can do math on, like HTTP's Content-Length

---

Chapter 1: the platform
	- tasks, actions, reactions, skills, loops, etc.
	- macOS app
	- iOS app
Chapter 2: multiplayer
	- the programmatic interface 
		- email as first class citizen
		- http api, open responses, websocket
	- shared resources
	- realtime collaboration
	- chat (DMs)
	- human verification
Pahse 3: 

meta/microsoft:

- spot market opportunity
- build dark pattern software around it
- burn money to monopolize that industry
- work as hard as possible to LOCK YOU IN

> China and US get unimaginably rich
> US starts buying poorest countries
> they do great
> small countries start to sell off to US
> a big chunk of that income flows to the new economy (distributed, decentralized, open source, etc)
> China starts expanding too
> new econ is already >15% of the global GDP

> US, China and new econ.

> US and China is all that left, weirdly distributed

	x Europe? North Korea? The arabs?

> all extreme shit (religions, centralized states, )

> our take into AI is like owning a car
> you OWN it, you do whatever the F you want with it, and you're responsible for the outcomes

---

- [Takes on Manus](../../../private/files/references/takes-on-manus/_index.md)

---

INTERNAL_JOKES.md

---

we could trash all instructions and do every thing example driven

we build examples with codex, say 15
codex uses those 10 to write instructions
then evaluates against all the 15 in a loop until all pass

for that whats the primitive?
do we need `learnings`, for `feedbacks` or `examples`?

---

> write examples from this convo down to disk
> raw, 1 file per feedback
> rewrite unclutter from it

---
if everything is a file, a task has a tag like kind:task or smth
all files have statuses etc

or maybe tasks are simply a directory
could work like macOS `.app`
`.task` is a directory that usually (convention) has
instructions.*
summary.*
any other files, including other .task/s
we could render meta as like `/meta.xml` or smth so its visible from our VFS
---
tags could have an optional description and even schema enforcement
like, values must comply to a certain schema
---
`preferences` can be just a tag

for  `references/` it could be a "global" (direct child to root) task

---

add support for ACP https://agentclientprotocol.com/get-started/introduction

---

we could embed a web vscode with our virtual worktree!

---

consider for secrets https://www.convex.dev/components/convex-secret-store

---

consider for analytics replacement https://www.convex.dev/components/abdssamie/convex-analytics

---

convex collaboratative editor prosemirror reference: https://www.convex.dev/components/prosemirror-sync

---

A Tracer component for Convex. Powerful Observability and tracing for Convex applications. Track function calls across queries, mutations, and actions with detailed insights, nested spans, and automa
https://www.convex.dev/components/convex-tracer

---
reference for convex passkey auth
https://www.convex.dev/components/convex-passkey-auth

