---
title: Create a skill for Meseeks own repo access
tags: [status:backlog, class:task, skill]
---

Create a skill that gives Meseeks reliable access to an up-to-date copy of its own codebase, so it can understand how itself behaves.

Original note:

> what if meseeks always had an accesible up to date codebase of itself, so it could understand how itself behaves?
