{
	"name": "primate-warfare",
	"private": true,
	"version": "0.1.0",
	"type": "module",
	"scripts": {
		"dev": "bun --bun vite --host 0.0.0.0 --port 3022",
		"build": "tsc --noEmit && bun --bun vite build",
		"typecheck": "tsc --noEmit",
		"preview": "bun --bun vite preview --host 0.0.0.0 --port 3022"
	},
	"dependencies": {
		"three": "^0.184.0"
	},
	"devDependencies": {
		"@types/three": "^0.184.0",
		"typescript": "^6.0.3",
		"vite": "^7.3.2"
	}
}
